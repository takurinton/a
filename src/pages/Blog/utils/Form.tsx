export const Form = ({
  id,
  title,
  is_open,
  contents,
  pub_date,
}: {
  id: number;
  title: string;
  is_open: boolean;
  contents: string;
  pub_date: string;
}) => {
  return (
    <>hoge</>
  );
};